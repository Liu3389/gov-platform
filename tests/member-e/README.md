# 成员 E（前端）测试脚本目录
# 负责: gov-frontend（Vue3 + Vite，端口 5173）
#
# 前端页面测试清单：
#   - test-login.spec.ts       → 登录页面测试（Vitest/Playwright）
#   - test-dashboard.spec.ts   → 首页仪表盘测试
#
# API 调用测试（可选）：
#   - test-frontend-apis.sh    → 前端调用的后端 API 验证
#
# AI 提示：让 AI 读取 .trae/rules/tasks/frontend.md 后生成测试
