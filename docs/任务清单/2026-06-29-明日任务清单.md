# 智慧政务一体化便民服务平台 — 明日任务清单

> 日期：2026-06-29 | 组长：组员A | 全队5人

---

## 前置准备（全员）

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 重建数据库（获取最新测试数据）
bash scripts/reset-db.sh

# 3. 编译全项目
cd gov-platform-parent && mvn compile -DskipTests

# 4. 启动基础设施
cd docker && docker compose up -d
```

---

## 一、组长（兼组员A）

> 模块：gov-gateway(8091) + gov-common + gov-user(8081) + gov-item(8092) + gov-monitor(8090)  
> 分支：feature/member-a

### 今日任务

| # | 任务 | 优先级 | 预计耗时 |
|---|------|:---:|:---:|
| A1 | **网关 CORS 配置**：在网关 application-dev.yml 添加 `allowedOriginPatterns: http://localhost:5173`，使前端（成员E）能正常调用 API | 🔴 高 | 15min |
| A2 | **用户登录接口验证**：确认 seed 数据用户（user100-user149，密码占位符 `$2a$10$placeholder`）的登录机制。当前登录返回400"密码错误"，需检查 bcrypt 密码哈希是否正确 | 🔴 高 | 30min |
| A3 | **全部分页接口 pageSize=0 回归**：执行 `bash test_all_apis.sh`，确认 35 个 Controller 的 @Min(1) 生效，pageSize=0 返回 400 而非 500 | 🔴 高 | 20min |
| A4 | **Knife4j 聚合文档验证**：确认 http://localhost:8091/doc.html 上所有服务接口可见，swagger-config 正确注册 | 🟡 中 | 20min |
| A5 | **Seed 数据补充**：在 `scripts/gen_seed.py` 中补充边界场景（空列表查询、跨页查询、不存在 ID 查询）和状态流转数据（各状态值覆盖） | 🟡 中 | 1h |
| A6 | **跨服务 Feign 联调**：启动所有服务，验证 user→reception、reception→activiti、license→message 的 Feign 调用链完整（FallbackFactory 不降级） | 🟢 低 | 1h |

### 验证标准

- [ ] 网关 CORS 生效：浏览器 `fetch('http://localhost:8091/api/v1/user/list')` 不报跨域错误
- [ ] 用户登录返回 200 + JWT Token
- [ ] `pageSize=0` 返回 `{"code":400,"message":"最小不能小于1"}`
- [ ] Knife4j 全部服务文档可访问
- [ ] `_test_all.py` 全部 PASS

---

## 二、成员B

> 模块：gov-reception-service(8083) + gov-open-service(8087)  
> 分支：feature/member-b

### 今日任务

| # | 任务 | 优先级 | 预计耗时 |
|---|------|:---:|:---:|
| B1 | **编译验证 + 启动服务**：`mvn compile -DskipTests`，启动 reception + open 两个服务，确认 Nacos 注册成功 | 🔴 高 | 15min |
| B2 | **受理模块全量 CRUD 测试**：测试 t_reception_record 的增删改查 + 分页 + 关键词搜索 + 状态筛选 | 🔴 高 | 30min |
| B3 | **公开模块全量 CRUD 测试**：测试 t_open_notice/policy/apply 的增删改查 + 分页 + 鉴权 | 🔴 高 | 30min |
| B4 | **pageSize 校验测试**：所有分页接口传 pageSize=0 应返回 400，pageSize=101 应返回 400 | 🔴 高 | 15min |
| B5 | **Feign 调用链验证**：reception 调用 user（获取用户信息）和 item（获取事项信息）的 Feign 是否正常 | 🟡 中 | 30min |
| B6 | **鉴权测试**：无 Token 访问 /api/v1/reception/* 和 /api/v1/open/* 应返回 401 | 🟡 中 | 15min |
| B7 | **公开接口白名单验证**：/api/v1/open/notice 和 /api/v1/open/policy 的 GET 查询应无需 Token 可访问 | 🟡 中 | 15min |

### 验证标准

- [ ] 两个服务 Nacos 注册状态 UP
- [ ] 受理 CRUD 全部返回 200，数据内容正确
- [ ] 公开 CRUD 全部返回 200
- [ ] pageSize=0 → 400
- [ ] 无 Token → 401
- [ ] 公开接口白名单生效（无需 Token 可 GET）

---

## 三、成员C

> 模块：gov-activiti-service(8084) + gov-license-service(8085)  
> 分支：feature/member-c

### 今日任务

| # | 任务 | 优先级 | 预计耗时 |
|---|------|:---:|:---:|
| C1 | **编译验证 + 启动服务**：`mvn compile -DskipTests`，启动 activiti + license 两个服务 | 🔴 高 | 15min |
| C2 | **权限鉴权全量测试**：执行 `python3 test_activiti_full.py`，确认 admin 可操作所有接口（200），普通用户被拒绝（403） | 🔴 高 | 30min |
| C3 | **流程启动联调**：用 seed 数据流程定义 `apply_approval_v1`，通过网关 /api/v1/workflow/start 启动真实流程实例 | 🔴 高 | 30min |
| C4 | **证照模块 CRUD 测试**：t_license_data 的增删改查 + 分页 + 证照模板查询 + 签章记录查询 | 🟡 中 | 30min |
| C5 | **证照生成接口测试**：POST /api/v1/license/generate，验证返回 LicenseVO 字段完整（licenseNo、qrCode、fileUrl 等） | 🟡 中 | 20min |
| C6 | **审批人分配测试**：验证流程定义中每个任务节点的 assignee 或 candidateGroups 正确配置 | 🟢 低 | 30min |

### 验证标准

- [ ] 两个服务 Nacos 注册状态 UP
- [ ] `test_activiti_full.py` 全部 PASS（20/20 或类似）
- [ ] 流程启动成功，返回 processInstanceId
- [ ] 证照 CRUD 全部 200
- [ ] 证照生成返回完整 LicenseVO

---

## 四、成员D

> 模块：gov-complaint-service(8086) + gov-datashare-service(8088) + gov-message-service(8089)  
> 分支：feature/member-d

### 今日任务

| # | 任务 | 优先级 | 预计耗时 |
|---|------|:---:|:---:|
| D1 | **编译验证 + 启动服务**：`mvn compile -DskipTests`，启动 complaint + datashare + message 三个服务 | 🔴 高 | 15min |
| D2 | **投诉模块全量测试**：按 `test_all_apis.sh` 的 complaint 部分，测试 Work/Handle/Suggestion/Hotspot/Supervise/Category 全部 Controller | 🔴 高 | 40min |
| D3 | **pageSize 校验测试**：所有分页接口 pageSize=0→400，pageSize=101→400 | 🔴 高 | 15min |
| D4 | **数据共享模块测试**：t_share_api 的 CRUD + 订阅授权 + 交换日志查询 | 🟡 中 | 30min |
| D5 | **消息通知模块测试**：消息模板管理 + 消息记录查询 + RabbitMQ 发送验证 | 🟡 中 | 30min |
| D6 | **鉴权测试**：无 Token 访问三个服务接口均返回 401 | 🟡 中 | 15min |
| D7 | **投诉状态流转测试**：工单 待受理→处理中→已办结→已评价 完整链路 | 🟢 低 | 30min |

### 验证标准

- [ ] 三个服务 Nacos 注册状态 UP
- [ ] 投诉全部接口 200，数据内容正确
- [ ] 共享 API 注册/订阅/日志功能正常
- [ ] 消息模板/记录查询正常
- [ ] 所有 pageSize=0 → 400
- [ ] 无 Token → 401

---

## 五、成员E（前端）

> 模块：gov-frontend（Vue3 + Vite + TypeScript）端口 5173  
> 分支：feature/member-e

### 今日任务

| # | 任务 | 优先级 | 预计耗时 |
|---|------|:---:|:---:|
| E1 | **项目初始化**：`npm create vite@latest gov-frontend -- --template vue-ts`，安装依赖（axios、pinia、vue-router、element-plus），启动 dev server | 🔴 高 | 20min |
| E2 | **Axios 封装**：按 `.trae/rules/tasks/frontend.md` 创建 `src/api/index.ts`，配置 baseURL=http://localhost:8091/api/v1 + 请求拦截器（自动带 Token）+ 响应拦截器（401 跳登录） | 🔴 高 | 30min |
| E3 | **登录页面**：创建 `src/views/login/LoginView.vue`，表单（用户名+密码），调用 /api/v1/user/login，成功后将 Token 存 localStorage + Pinia，跳转首页 | 🔴 高 | 1h |
| E4 | **导航守卫**：router.beforeEach 检查 localStorage 有无 Token，无则重定向 /login；401 拦截自动清除 Token 跳登录 | 🔴 高 | 30min |
| E5 | **用户列表页**：创建 `src/views/user/UserListView.vue`，调用 /api/v1/user/list 分页表格展示（Element Plus el-table + el-pagination） | 🔴 高 | 1h |
| E6 | **用户 API 模块**：创建 `src/api/user.ts`，封装 getList/getById/add/update/delete 5 个方法 | 🟡 中 | 20min |
| E7 | **布局框架**：创建 MainLayout（侧边栏导航 + 顶部栏 + router-view），侧边栏菜单项：工作台/用户管理/部门管理/角色管理 | 🟡 中 | 1h |
| E8 | **验证前后端联通**：浏览器访问 http://localhost:5173，Network 面板确认 API 调用 200/401 正常 | 🟡 中 | 15min |

### 验证标准

- [ ] `npm run dev` 启动成功，http://localhost:5173 可访问
- [ ] 登录页输入任意用户 → Network 面板可见 POST /api/v1/user/login
- [ ] 登录成功 → Token 存 localStorage → 跳转首页
- [ ] 无 Token 直接访问 /dashboard → 重定向 /login
- [ ] 用户列表页分页切换 → Network 面板可见 GET /user/list?pageNum=2&pageSize=10
- [ ] 不直接写 `fetch()` —— 全部用 `src/api/` 封装

---

## 六、每日检查（组长执行）

| 时间 | 检查项 |
|------|--------|
| 09:00 | 全员 git pull + docker compose up -d，确认 Docker 基础服务运行 |
| 10:00 | 各组员汇报编译状态，确认 Nacos 全部服务 UP |
| 12:00 | 上午进度汇报：各组员列出完成/进行中/受阻 |
| 17:00 | 下午收尾：各组员 mvn compile 验证 → 提交代码到各自 feature 分支 |
| 18:00 | 组长汇总：检查各分支代码 → 合并到 main → 全量回归测试 |

---

## 七、已知问题跟踪

| # | 问题 | 状态 | 负责人 | 备注 |
|---|------|:---:|:---:|------|
| 1 | 用户登录密码 bcrypt 占位符，seed 用户无法登录 | ⏳ 待修复 | 组长 | 需生成真实 BCrypt 哈希替换 `$2a$10$placeholder` |
| 2 | 测试数据覆盖不全，缺少边界和异常场景 | ⏳ 待补充 | 组长 | 需在 gen_seed.py 中补充 |
| 3 | Sentinel 日志目录权限问题导致 ExceptionInInitializerError | ✅ 已修复 | 组长 | 已创建 /Users/xxx/logs/csp/ 目录 |
| 4 | 成员E 前端项目尚未初始化 | ⏳ 待创建 | 成员E | 今日 E1 任务 |

---

## 八、代码提交规范

```
git add {只加自己模块的目录}
git commit -m "feat(模块): 描述"
git push origin feature/member-x
```

提交后去 GitHub 创建 PR：
- base: `main` ← compare: `feature/member-x`
- 描述模板包含：改动模块、改动内容、验证结果（编译/测试）、AI-assisted: true
